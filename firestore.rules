rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Izinkan semua akses untuk pengembangan
      allow read, write: if true;
      
      // Gunakan rule ini untuk produksi
      // allow read, write: if request.auth != null;
    }
  }
}